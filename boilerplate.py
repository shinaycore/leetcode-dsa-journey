"""
Problem   : <Problem Name> (<Difficulty>)
Topic     : <Topic, e.g. Arrays / Sliding Window / DP>
Link      : https://leetcode.com/problems/<problem-slug>/

Approach:
    <1-3 lines describing the core idea/algorithm used>

Brute Force (if applicable):
    <1-2 lines on the naive approach and why it's worse>

Edge Cases Considered:
    - <e.g. empty input>
    - <e.g. single element>
    - <e.g. duplicates / negatives / overflow, etc.>

Time Complexity  : O(?)
Space Complexity : O(?)
"""

class Solution:
    def solve(self, *args, **kwargs):
        """
        Rename this method to match the problem's actual signature
        (e.g. twoSum, maxSubArray, lengthOfLongestSubstring) when you
        copy this in — keeping `solve` here only as a placeholder.

        This is the OPTIMIZED solution.
        """
        pass